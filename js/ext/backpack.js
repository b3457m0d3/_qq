define(["jquery",'X','backbone','associate'], function($,_,Backbone) {
  var mixins = {
    
  };
  var BackPack = Backbone.Model.extend(_.extend({}, mixins, {

  // (Methods and attributes here)

  }));

});
